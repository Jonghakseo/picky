import SwiftUI

struct Example: View {
    var body: some View {
        Text("semantic")
            .padding(DS.Spacing.space1)
            .background(RoundedRectangle(cornerRadius: DS.CornerRadius.control))
    }
}
